# Dayblog

Notion 일기 DB의 오늘자 페이지를 한 명령으로 Hugo(GitHub Pages) 포스트로 발행하는 개인용 파이프라인. Self-dogfood 전용, PyPI 배포 없음.

대상 Hugo 사이트: `D:\vscodeprojects\blog` (PaperMod).

## 설치

```bash
pip install -e .[mcp,dev]
```

## 현재 상태

Phase 0 (스켈레톤). 상세는 [docs/domain-notes.md](docs/domain-notes.md) 참고.
